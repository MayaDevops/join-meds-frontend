import { ksmThemeStyles } from 'common/components';

const { colors } = ksmThemeStyles;

const dark = colors.tertiary[500];
const primary = colors.primary[500];
const secondary = colors.secondary[500];
const light = colors.containerColor;
const notes = '#F0F4F7';

export {
  dark,
  primary,
  secondary,
  light,
  notes
};
